# Request Artifacts

This folder is for review-only request artifacts created during private workshop runs.

Do not commit customer-specific request outputs, tenant identifiers, subscription identifiers, workspace names, resource IDs, or live traffic findings to the public repository.

Recommended practice:

- keep request folders local or in a private branch
- sanitize any example artifacts before publishing
- remove generated request outputs before merging to a public branch